{
  "name": "react-xml-component",
  "version": "1.0.0",
  "description": "Simple react component for rendering custom tags",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --config jest.config.json",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "ci-local": "npm test && npm run lint && npm run build -- --noEmit",
    "ci": "npm run test -- --coverage && npm run lint && npm run build -- --noEmit"
  },
  "author": "Konstantin Smirnov <thgala@gmail.com>",
  "engines": {
    "node": ">=10.0.0"
  },
  "files": [
    "dist/"
  ],
  "devDependencies": {
    "@types/jest": "^23.3.10",
    "@types/node": "^10.12.15",
    "jest": "^23.6.0",
    "jest-junit-reporter": "^1.1.0",
    "ts-jest": "^23.10.5",
    "tslint": "^5.11.0",
    "tslint-config-airbnb": "^5.11.1",
    "typescript": "^3.2.2"
  },
  "dependencies": {}
}
